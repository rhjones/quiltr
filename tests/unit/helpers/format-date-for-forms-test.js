
import { formatDateForForms } from 'quiltr/helpers/format-date-for-forms';
import { module, test } from 'qunit';

module('Unit | Helper | format date for forms');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDateForForms([42]);
  assert.ok(result);
});

