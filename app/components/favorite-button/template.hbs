{{#if isAuthenticated}}
  {{#if favorite}}
    <i title="Favorite this pattern" class="fa fa-heart" aria-hidden="true" {{ action 'toggleFavorite'}}></i>
  {{else}}
    <i title="Favorite this pattern" class="fa fa-heart-o" aria-hidden="true" {{ action 'toggleFavorite'}}></i>
  {{/if}}
{{/if}}